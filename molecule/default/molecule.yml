---
dependency:
  name: galaxy
driver:
  name: podman
platforms:
  - name: instance
    image: localhost/sle-bci-init-ansible
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
